name: ui_kit
description: Landfall design system — colors, typography, spacing, base widgets.
publish_to: none

environment:
  sdk: "^3.8.0"
  flutter: ">=3.29.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^8.1.0
  landfall_shared:
    path: ../landfall_shared

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
