name: landfall_shared
description: Shared Dart models for Landfall server and client.
publish_to: none

environment:
  sdk: "^3.8.0"

resolution: workspace

dev_dependencies:
  lints: ">=6.0.0 <7.0.0"
  test: ^1.25.5
