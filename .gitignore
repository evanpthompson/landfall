# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
logs/

# Serverpod generated files are committed — CI has no serverpod CLI.
# Drift / build_runner generated files are committed for the same reason.
# Run `serverpod generate` or `dart run build_runner build` locally after
# changing model files, then commit the result.


# IntelliJ / Android Studio
.idea/
*.iml
*.ipr
*.iws

# VS Code
.vscode/

# macOS
.DS_Store

# Environment / secrets
.env
*.env.local
*.pem
config/passwords.yaml

# OpenTofu / Terraform
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.backup
infra/.terraform.lock.hcl

# Melos
.melos_tool/

# Claude Code
.claude/

# Internal docs (kept in ~/files/automation/landfall)
docs/app_failure_log.md
docs/competitive_analysis.md
docs/known_bugs.md
docs/security_hardening_phase.md

# Python
__pycache__/
*.pyc
*.pyo

# Coverage
coverage/
lcov.info

# Golden test failure diffs (auto-generated, machine-specific)
**/test/**/failures/

# Pi image build artifacts (cloned at runtime by deploy/pi-gen/build.sh)
deploy/pi-gen/work/
deploy/pi-gen/landfall-build.conf
